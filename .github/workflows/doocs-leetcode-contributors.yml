name: Contributors
on:
  schedule:
    - cron: '0/10 * * * *'
  push:
    branches:
      - main
jobs:
  contributors:
    runs-on: ubuntu-latest
    steps:
      - uses: bubkoo/contributors-list@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          round: true
          svgPath: DOOCS_LEETCODE_CONTRIBUTORS.svg
